let count = 0;
function countValue(array) {
//   console.log(array);
  for (number of array) {
    if (number === 
      5) {
      count++;
    }
  }
  return count;
}
const array = [21, 5, 12, 5, 98];
const mainFunction = countValue(array);
console.log(mainFunction);
